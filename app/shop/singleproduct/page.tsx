'use client'

import React from 'react'
import { usePathname } from 'next/navigation'
import { BreadCrumb, PageHeader } from '@/components/shared'

const Page = () => {
    return <PageHeader title='product details' />
}

export default Page
